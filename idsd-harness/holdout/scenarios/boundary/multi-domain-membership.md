# 边界场景：群同时属于多个域

**场景**：一个群同时加入了两个不同的域（域 A 和域 B）。

**期望行为**：
1. 每个域独立维护该群的信誉和能力信息
2. 群在域 A 的信誉变化不影响域 B 中的信誉
3. 群可以向两个域分别注册不同的能力声明
4. 域 A 和域 B 之间不共享该群的信息

**验证方式**：
- 让同一个群分别注册到两个域
- 在两个域中分别查询该群的能力
- 验证两个域的数据互不干扰
