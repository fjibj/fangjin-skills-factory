---
{
  "checks": []
}
---
# 失败场景（MANUAL）：域节点宕机

**场景**：域节点宕机后恢复。

**期望行为**：
1. 注册数据不丢失
2. 恢复后自动重建索引
3. 宕机期间发出的请求在恢复后能被正确处理

> 本场景无 checks —— 评估器自动标记 **MANUAL**，需人工验证，不参与通过率。
> 适用于无法用 API 断言表达的场景（宕机、视觉、UX、端到端人工流程）。
